name: dart_obfuscator
description: B面代码混淆工具 - 使用 Dart AST 分析实现精确混淆
version: 1.0.0

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  analyzer: ^6.0.0
  args: ^2.4.0
  path: ^1.8.0
  crypto: ^3.0.0

dev_dependencies:
  lints: ^3.0.0
