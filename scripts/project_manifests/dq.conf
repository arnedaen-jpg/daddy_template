# =============================================
#   dq 项目（斗球/直播）Framework 混淆清单
#   Pod 名与版本须与「壳工程」ios/Podfile.lock 一致（flutter pub get + ios/pod install 后对照）
#   样本: animal_match @ 2026-04-25（见同目录说明）
#   亦可用: ./scripts/obfuscate_frameworks.sh list -p dq 查看 B 面 pub 插件；以 Pod 为准定版
# =============================================
# 格式: type:name:version:level
# 注: Flutter 插件 Pod 在 lock 中常为 0.0.1（podspec 占位）；下方版本照抄 PODS: 段第一列

# === Flutter 插件 Pod（可重命名，L0） ===
remote:app_tracking_transparency:0.0.1:L0
remote:audioplayers_darwin:4.1.0:L0
remote:device_info_plus:0.0.1:L0
remote:just_audio:0.0.1:L0
remote:package_info_plus:0.4.5:L0
remote:qrcode_flutter:3.0.0:L0
remote:rongcloud_im_wrapper_plugin:5.32.6:L0
remote:umeng_common_sdk:0.0.1:L0
remote:vibration:1.7.5:L0
remote:video_player_avfoundation:0.0.1:L0
remote:video_thumbnail:0.0.1:L0
remote:webview_flutter_wkwebview:0.0.1:L0

# === Flutter 插件 Pod / 传递侧（仅变异或低阶，L1） ===
remote:audio_session:0.2.3:L1
remote:connectivity_plus:6.1.0:L1
remote:image_picker_ios:0.8.13+3:L1
remote:path_provider_foundation:0.0.1:L1
remote:permission_handler_apple:9.1.1:L1
remote:shared_preferences_foundation:0.0.1:L1
remote:sqflite_darwin:2.4.2:L1
remote:url_launcher_ios:0.0.1:L1

# === 本地依赖（plugins/ path 引用） ===
# fijkplayer  —— B 站魔改 IjkPlayer (path: plugins/fijkplayer/)，L3 + 专属 profile
#                覆盖 ios/Classes/ 下 ObjC 桥接代码；IJKFFMediaPlayer C 内核来自
#                预编译 BIJKPlayer.framework，本层不触碰
# geetest_sdk —— 极验滑块验证 (path: plugins/geetest_sdk/)，含 swift+dart 源，保守 L0
#                避免触发 SDK 自检（滑块验证常含防篡改），不动 SDK 自身符号
local:fijkplayer::L3
local:geetest_sdk::L0

# === 第三方 CocoaPods（二进制/不可修改，对齐 51cg/md/tiktok/yms 业界惯例） ===
# 友盟 SDK 是预编译 .xcframework，无 .m/.swift 源，profile 无效；
# libwebp 是 Google C 源码，脚本只识别 .m/.swift，进入 mutate_pod 也会"无源码 跳过"；
# BIJKPlayer / RongCloudIM 同样是预编译，标 disabled 表达准确语义。
# 若以后增加二进制扰动工具链（Mach-O 字符串修改 / reserved sections）再单独处理。
pod:BIJKPlayer:0.7.16:disabled
pod:RongCloudIM:5.36.4:disabled
pod:UMCommon:7.5.10:disabled
pod:UMDevice:3.5.0:disabled
pod:libwebp:1.5.0:disabled
